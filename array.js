let name = ("christian", "cj", "sophie");
function addingArray(array){ 
  return name.push(array);
}

addingArray("james")

function addingArray(names, name) {
  names.push(name)
  return names
}

addingArray(names, "lisa")